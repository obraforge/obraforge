// Códigos de saída da CLI (item C1 do plano da fase 0; o 2 passa a ser usado no add da fase 1).
export const EXIT_SUCCESS = 0;
export const EXIT_USAGE_ERROR = 1;
// Recusa de segurança: hash divergente, skill retirada, link simbólico no caminho de destino.
export const EXIT_SECURITY_REFUSAL = 2;
export const EXIT_ENVIRONMENT_ERROR = 3;
