// Códigos de saída da CLI (item C1 do plano da fase 0).
export const EXIT_SUCCESS = 0;
export const EXIT_USAGE_ERROR = 1;
// Reservado para recusa de segurança (hash divergente, skill retirada). Sem uso na fase 0.
export const EXIT_SECURITY_REFUSAL = 2;
export const EXIT_ENVIRONMENT_ERROR = 3;
