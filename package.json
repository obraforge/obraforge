{
  "name": "obraforge-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "cli",
    "tools"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "validar": "npm run build --workspace tools && node tools/dist/src/validate-cli.js skills",
    "catalogo": "npm run build --workspace tools && node tools/dist/src/catalog-cli.js"
  },
  "devDependencies": {
    "@types/node": "26.6.1",
    "typescript": "7.0.2"
  }
}
