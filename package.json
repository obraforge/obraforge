{
  "name": "obraforge-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "cli",
    "tools"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "validar": "npm run build --workspace tools && node tools/dist/src/validate-cli.js skills"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "typescript": "7.0.2"
  }
}
